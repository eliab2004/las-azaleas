import {db,identity,member,fail} from '../../../lib/server';
export async function GET(){try{const u=await identity();const count=await db().prepare('SELECT count(*) n FROM members').first<any>();if(!count.n)return Response.json({setup:true,email:u.email},{headers:{'Cache-Control':'no-store'}});const m=await member();const seller=m.role==='vendedor'||m.role==='asesor';const branches=await db().prepare(seller?'SELECT * FROM branches WHERE id=?':'SELECT * FROM branches').bind(...(seller?[m.branch_id]:[])).all();const projects=await db().prepare(seller?'SELECT * FROM projects WHERE branch_id=?':'SELECT * FROM projects').bind(...(seller?[m.branch_id]:[])).all();const lots=await db().prepare(seller?'SELECT l.* FROM lots l JOIN projects p ON l.project_id=p.id WHERE p.branch_id=?':'SELECT * FROM lots').bind(...(seller?[m.branch_id]:[])).all();const clients=m.role==='secretaria'?(await db().prepare('SELECT r.*,l.code,p.name project FROM reservations r JOIN lots l ON l.id=r.lot_id JOIN projects p ON p.id=l.project_id ORDER BY r.created_at DESC').all()).results:[];const users=m.role==='administrador'?(await db().prepare('SELECT * FROM members').all()).results:[];return Response.json({member:m,branches:branches.results,projects:projects.results,lots:lots.results,clients,users},{headers:{'Cache-Control':'no-store'}});}catch(e){return fail(e)}}
